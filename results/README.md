# Results Directory

Offline CSI extraction and analysis outputs are written under
`results/<capture_id>/`.

Typical result files:

```text
H.npy
csi_info.json
analysis_summary.json
pilot_repeat_consistency.json
report.md
```

This directory is ignored by Git except for this README and `.gitkeep`.
