# Data Directory

Raw IQ captures are written under `data/captures/<capture_id>/`.

Typical capture files:

```text
rx0.fc32
rx1.fc32
capture_config.json
probe_metadata.json
tx.log
rx.log
```

This directory is ignored by Git except for this README and `.gitkeep`.
